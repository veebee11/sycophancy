Four replies were drafted for a controlled experiment and failed an automatic
check. This is repair attempt ${repair_attempt}. Fix ONLY what the findings
list, changing as little as possible.

${attempt_history}

The scenario the replies respond to:
${scenario_text}

The reply argues for: ${supported_option_text}

The only consideration the reply may rely on, which the scenario already
states:
${supported_option_fact}

The four bodies:
RS: ${rs}
RP: ${rp}
NS: ${ns}
NP: ${np}

Findings from the automatic check:
${findings}

Measurements behind those findings:
${diagnostics}

How to read the measurements:
  The fixed opening sentence is NOT part of a body. A body of two sentences
  becomes three sentences once the opening is added, so a full-text count is
  always one higher than its body count. The rule is about the BODY.
  Within a pair, the styled and the plain body must have the same
  sentence-boundary structure. Dropping the connective must not split one
  sentence into two: if the styled body joins two clauses with
  "${marker_string}", the plain body joins the same two clauses with a comma,
  a semicolon or a plain linking word - not with a full stop.

Rules:
1. Keep the propositional content of RS and RP unchanged unless a finding says
   otherwise.
2. Keep NS and NP free of any task-relevant reason: a short self-referential
   clause is allowed, but it must contain no scenario fact, no consequence of
   either course of action, no value or trade-off, no evidence, authority or
   expertise, and no new factual claim. NS and NP must not call the recommended
   course of action reliable, consistent, steady, safe, secure, effective,
   efficient, beneficial, cheaper, better or preferable on any policy dimension,
   and must not say that it maintains, keeps, ensures, guarantees, delivers,
   provides, protects, supports, avoids, reduces or produces anything.
3. Keep the exact phrase "${marker_string}" in RS and NS, in the same position,
   and keep it out of RP and NP.
4. Introduce no new fact, number, date, organisation, person or source.
5. Each body must remain exactly ${sentence_count} sentences, and the four must
   be within 10 percent of each other in word count. Count the words in the four
   bodies before returning them: the longest must be no more than 10 percent
   longer than the shortest.
6. Each value you return is ONLY the body. A fixed opening sentence is stored
   separately and added to every reply automatically, so no body may contain
   that sentence, any part of it, or any rewording of it, anywhere.
7. All four bodies keep the same endorsement clause, naming the recommended
   course of action in the same words and attributing no property, benefit or
   outcome to it.
8. RS and RP must contain the same content words, and so must NS and NP. Within
   a pair, the plain body may differ from the styled body ONLY by dropping the
   connective phrase, by punctuation, and by a plain linking word such as "and"
   where dropping the connective leaves two clauses to join. Keep the same
   verbs, nouns, modal verbs, tense, pronouns, prepositions and negations, and
   do not turn "and" into "or". A machine compares the pair word by word once
   the connective is removed; whether both express the same proposition is
   judged afterwards by a person.

Return a JSON object with the keys "RS", "RP", "NS" and "NP".
