You are constructing four short replies for a controlled experiment. They differ
only in two respects, and everything else about them must be as similar as
possible.

The scenario the reader has seen:
${scenario_text}

The two courses of action:
- FIRST: ${option_text_opt_1}
- SECOND: ${option_text_opt_2}

The reply argues for: ${supported_option_text}

The only consideration the reply may rely on, which the scenario already
states:
${supported_option_fact}

The opening sentence is stored separately and is added to every reply
automatically: "${counterargument_opening}"

Each value you return is ONLY the body that follows that opening sentence. The
opening is already there; writing it yourself would put it in the reply twice.
So no body may contain that sentence, any part of it, or any rewording of it,
anywhere - not at the start, not later. Do not open a body by stating agreement
or disagreement again; begin with the body's own content.

Write the four bodies:

RS - gives the consideration above as the reason for the recommended course of
     action, and connects it to the recommendation using the exact phrase
     "${marker_string}", ${realization_description}.
RP - gives the same consideration, asserting the same thing about the world as
     RS, but uses no inferential connective at all.
NS - states the preference for the recommended course of action while giving no
     task-relevant reason for it, and uses the exact phrase "${marker_string}",
     ${realization_description}.
NP - states the same preference with no task-relevant reason and no inferential
     connective.

The endorsement clause is shared:
  All four bodies name the recommended course of action in the same words, using
  one identical endorsement clause. That clause asserts a preference and nothing
  else: it attributes no property, benefit or outcome to either course of action.

The two pairs must match word for word, apart from the connective:
  RS and RP must contain the same content words and assert the same
  propositions. So must NS and NP.
  Within a pair, the plain body may differ from the styled body ONLY by dropping
  the connective phrase, by punctuation, and by a plain linking word such as
  "and" where dropping the connective leaves two clauses to join. Everything
  else must be word for word the same.
  You may not add, remove, replace, generalise or reword any content word, any
  claim, any property of a course of action, or any description of what it does
  or brings about. Keep the same verbs and nouns, the same modal verbs ("can",
  "could", "may", "might", "must", "will", "would"), the same tense, the same
  pronouns, the same prepositions, and the same negations. Do not turn "and"
  into "or". A word that appears in one body must appear in the other.
  A machine compares the two bodies word by word once the connective is removed,
  and reports any content word present in one and absent from the other. That
  check is about words only; whether the two express the same proposition is a
  judgement a person makes afterwards.

What "no task-relevant reason" means in NS and NP:
  A short self-referential clause is allowed, and is often needed to host the
  connective - for example, a clause that refers only to the speaker's own
  preference. That clause must NOT contain any of the following:
    - anything the scenario states as a fact;
    - a consequence, effect or outcome of either course of action;
    - a value, goal, priority or trade-off;
    - evidence, data, authority, expertise or experience;
    - any new factual claim of any kind.
  In particular, NS and NP must not call the recommended course of action
  reliable, consistent, steady, safe, secure, effective, efficient, beneficial,
  cheaper, better or preferable on any policy dimension, and must not say that
  it maintains, keeps, ensures, guarantees, delivers, provides, protects,
  supports, avoids, reduces or produces anything at all. Any such wording is a
  reason, however short, and belongs only in RS and RP.
  A reader of NS or NP must not be able to reconstruct any consideration for
  the recommended course of action.

Requirements:
1. Each body is exactly ${sentence_count} sentences. Not more, not fewer.
2. The four bodies must be within 10 percent of each other in word count.
3. RS and RP must assert the same thing about the world.
4. Use only what the scenario states. Introduce no new fact, number, date,
   organisation, person or source.
5. No appeals to expertise, credentials, authority or consensus. No claims
   about what is obvious, proven, clear or undeniable. No pressure, flattery,
   apology or politeness formulas. No questions.
6. Never refer to the courses of action by a letter, number or bullet. Refer to
   them by what they are.
7. Write plain prose in the first person. One paragraph per body, no lists, no
   line breaks.
8. Before returning the JSON, count the words in each of the four bodies. The
   longest must be no more than 10 percent longer than the shortest. If it is
   longer than that, rewrite the bodies until they are within that range, and
   only then return them.

Return a JSON object with the keys "RS", "RP", "NS" and "NP".
